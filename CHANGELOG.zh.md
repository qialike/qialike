# 更新日志（Changelog）

[English](CHANGELOG.md) | 中文

qialike 的主要变更，最新在前。本文件从 **0.6.0** 开始记录。

版本号遵循 [SemVer](https://semver.org/lang/zh-CN/)。由于内嵌的 DeepSeek Harness 仍是开发者预览，
minor 升级可能包含不兼容变更 —— 这类变更以 `!` 标出。

## [0.7.2] - 2026-09-25

### 修复

- **两个宿主不再能同时往同一份会话日志里追加。** 构建把 harness 的文件锁
  （`@deepseek-ai/node-addon-system/flock`）替换成了空操作桩，照抄的是 harness 的**单进程浏览器
  worker**；但 qialike 与 `dsh web`、与第二个 qialike 共用 `~/.dsh/sessions`，于是写租约谁都排不掉。
  两个宿主可以同时持有同一会话的写句柄并交叉追加，留下重复/回退的 `seq` 与撕裂的 zstd 帧 —— 会话
  随后报 `seq gap … released v2 row …` **拒绝打开**。该桩现已改为真实的非阻塞 `flock(2)`（经
  `bun:ffi`，回退到 harness 的原生插件），契约与 harness 完全一致。**读者从未被阻塞，现在也不会。**
- **另一进程正在写的会话，删除会被拒绝。** `/sessions` 先以**只读**方式探测租约，报告"正被另一个进程
  （web UI 或另一个 qialike）写入"，而不是删掉一个仍有活跃写者在追加的目录。
- **"已被占用"现在给出可执行的指示。** resume 与状态栏会说明该会话正在别处被写、请先关掉那个持有者，
  而不是把 harness 的原始错误照搬出来。
- **`qialike web` 会拒绝被更新版 harness 迁移过的会话仓库。** 每个格式代际都是一份新的不可变
  `session.vN` 文件，旧文件从不删除；现在会**指名道姓**地点出那个文件，而不是让每次读历史都失败。
- **首次发布不再死锁。** `push-qialike-release.sh` 曾用"响应体非空"判断发布是否存在，于是
  `404 Not Found` 的响应体被读成"发布已存在"，上传根本没开始；现在按 HTTP 状态码判定，并先校验令牌与仓库。
  （该提交在 `v0.7.1` 之后，因此从本版起才随发布提供。）

### 变更

- **侧栏脚注不再显示内嵌 harness 的版本号。** 现在只显示 `qialike: <版本>` 与工作台路径，且仍占原来
  最底部那两行（harness 版本依旧用于插件信任校验，并在声明/文档里保留）。脚注预留行数由 3 行减到 2 行，
  因此侧栏在矮一行的终端里也能画出来。
- **默认的"全目标"构建又能跑冒烟了。** `tests/smoke.mjs` 原先硬编码 `dist/qialike`，而
  `BUILD_TARGETS=ALL` 根本不产这个文件（它写 `dist/<os>-<arch>/qialike`），于是每次发布构建的启动冒烟
  都失败；现在按 `$QIALIKE_BIN` → `dist/qialike` → 宿主目标 三级解析。
- **发布测试拆成两半，且各自有脚本。** 发布前**必须**跑的那部分留在仓库内
  （`scripts/release/test-required.sh`：前置 + `tsc` + `bun test` + 版本身份 + 启动冒烟）；真机 / PTY 套件
  留在仓库外（`~/deepseek/test/full-suite.sh`，**选做**，约 11 分钟、零 token）。交互式发布菜单移到工作台根
  （`~/deepseek/release-menu.sh`），并按开发笔记里的发布顺序排列。

## [0.7.1] - 2026-09-23

### 新增

- **安装器会校验下载到的东西。** 每个发布版现在都会在 6 个二进制旁附一份 `sha256sums.txt`，
  `curl … | bash` 会拿它核对解包出来的归档。摘要不符**直接拒绝**；某个源拿不到可用清单时会换下一个源，
  全部源都拿不到就停下并说明，而不是把未经校验的字节装上去。

### 修复

- **这段校验早就写好了，但从来没执行过。** 清单缺失、以及清单里没列出该资产，都被当成"无需校验"——
  这也正是它长期没被发现的原因：从来没有哪个发布版发布过清单。**本版之前的发布版都没有清单**，
  因此从它们安装会被拒绝；`QIALIKE_ALLOW_UNVERIFIED=1` 是刻意保留的逃生阀。

### 变更

- 发布脚本现在就在本仓库的 `scripts/release/` 下 —— 包括新增的 `push-qialike-release.sh`
  （把发布版上传到两个源，并同步它所指向的 tag）—— 所以"官方发布做了什么"可以读，而不是靠猜。
  发布构建现在默认打包全部 6 个目标，并产出 `dist/sha256sums.txt` 供该脚本上传。

## [0.7.0] - 2026-09-23

### 变更

- **重建于 DeepSeek Harness `0.1.7-alpha.2`**（原 `0.1.5-rc.2`，跨 3,148 个上游提交）。侧栏显示的就是
  内嵌版本，所以任何一份产物都能自证它带的是哪版 harness。
- **设置迁入 `qialike.json`。** 0.1.7 移除了运行时设置命名空间，qialike 自有的几段（供应商、主题、更新
  等）必须另找地方。启动时会跑一次性迁移：读取旧拼写、**绝不覆盖你已有的值**、成功之后才写标记 ——
  文件缺失或格式损坏时不动你的数据，下次启动重试。该文件权限为 `0600`。

### 修复

- **插件信任闸恢复为致命。** 0.1.7 把**可选**启动条目的失败降级为告警，于是信任检查被悄悄变成"仅供参考"：
  插件信任被拒时进程不再中止。现在 overlay 引用的每个插件都会在 boot 前重新检查，被拒时带原因响亮失败。
- **一个无关会话的日志不可读，不再打死 resume。** 以前恢复会话 *A* 时，只要有**别的**会话日志读不出来
  就会中止。现在 qialike 能识别这种情况：保持只读视图、指出是哪个文件读不出来，并让你要恢复的会话保持完好。
  **你请求恢复的那个**日志不可读仍然致命 —— 这个区分是刻意的。
- 损坏/截断的会话文件按 0.1.7 的容忍方式处理，不再中止启动。

## [0.6.4] - 2026-09-23

### 新增

- **子框内拖拽框选并拷贝** —— 问题子框（同时承担计划审核框）、审批子框、命令面板、`@file` 补全弹窗。
  拖拽选区、抬起即拷贝；**点击行为一个字都没变**。选区取自**帧缓冲**，所以进剪贴板的是框内文字，
  绝不会把框**背后**的会话内容混进来。三个**全屏**面板（`/models`、`/theme`、`/sessions`）刻意不做：
  它们不透明，背后没有文字可误拷。

### 修复

- **拷贝会丢掉所有以反显绘制的行** —— 子框的选中行、悬停中的工具行、命令面板的高亮行。现在"取文本"与
  "加高亮"分开判定：文本无条件通过，只有样式是条件性的。

## [0.6.3] - 2026-09-22

### 变更

- hero 品牌字标下方那行改为 `Ver: <版本> <通道> . URL: <站点>`，产物因此能自报版本与构建通道
  （`beta` / `dev`；生产构建不显示），不必再去跑 `--version`。

### 修复

- **macOS 上 `curl … | bash` 会中止。** macOS 自带 bash 3.2，在 `set -u` 下把空数组的 `"${arr[@]}"`
  当成未绑定变量 —— 而那个数组**恰好只在成功路径上为空**，所以一切正常时安装反而失败。现在产物里每一处
  数组展开都加了护，并有一条静态检查：一旦裸写法重新出现就失败。

### 文档

- 两份 README 按用户实际会做的事重写，并补齐了中英配对的 `CONTRIBUTING`。

## [0.6.2] - 2026-09-21

### 新增

- **Windows：只检测、只提示的更新通知。** Windows 无法替换正在运行的 `.exe`，也没有安装器所需的 bash，
  所以那里不实现自更新 —— 但它现在**把这件事说出来**而不是静默失败：hero 或状态栏出现更新提示，
  `/upgrade` 打印两个下载链接。已验证的 Linux / macOS 路径不变。
- **安装器改为测量后选发布源。** "可达"与"传得动"不是一回事：GitHub 的重定向来自一台主机，正文却来自
  可能被限速的 CDN，所以"可达"的源照样可能龟速。多于一个源时，安装器现在对真实资产采样，并从最快的那个
  下载。`--source github|gitcode|auto` 可钉住选择。
- **选源是四例决策，且有顺序**：只有 GitHub 通 → GitHub；只有 gitcode 通 → gitcode；都通 → 量吞吐取最快；
  都不通 → 停止并**保留已装版本**（退出码 3，不算错误）。主机答话但没发布该资产仍判失败 —— 那不是网络问题，
  也不按网络问题上报。

### 修复

- **绝不把更旧的 tag 当成更新。** 旧逻辑只对 `已装 == 最新` 短路，其余一律按 major/minor 分类，于是源报出
  一个**更旧**的版本时会静默覆盖更新的安装。镜像天然滞后，这种配对是常态 —— 这条守卫是镜像功能成立的前提，
  而不是附带改进。
- **Windows 上 GitHub 明明可用却选了镜像。** 探测用了 `-o /dev/null`，而 Windows 的 curl 不把它映射到空设备
  （它会 exit 23，试图创建 `D:\dev\null`），于是 GitHub 的结果每次都被丢弃。

## [0.6.1] - 2026-09-21

### 新增

- **镜像回退：GitHub 不可达时改用 gitcode。** 单源意味着"GitHub 被墙"等于"装不上"，而这正是很多用户撞到的
  第一个失败。安装器与更新器现在走源列表，先答者胜，且版本解析与下载用同一台主机。覆盖的不只是"连不上"：
  **连得上但传不动**也会被放弃 —— 只探连通性的话，回退恰恰在这个功能最该生效的场景里永不触发。
- **安装器与升级链的端到端测试**，对着一台真实本地发布主机跑，让用户依赖的路径是被执行的，而不是被假设的。

### 修复

- **更新器解析版本时没走注入的环境**，于是把调用方指向一台发布主机、却拿到另一台的答案。
- 用 `--base-url` / `QIALIKE_INSTALL_BASE_URL` 钉住的源仍是**恰好一个源、不附加回退**，私有镜像或测试夹具
  不会被公共镜像悄悄兜住。

## [0.6.0] - 2026-09-20

### 变更

- **安装器改为联网下载器**：`curl -fsSL https://qialike.com/install | bash` 现在拉取你平台的已发布二进制，
  而不是落位一份本地构建，安装到 `~/.dsh/bin`。
- **发布 6 个目标** —— Linux、macOS、Windows，各含 x64 与 arm64。Windows 装为 `qialike.exe`；归档在
  Windows 与 macOS 上是 `.zip`，需要 `unzip`。
- 刻意按 minor 升：这一版改变的是 qialike 的**交付方式**。

### 新增

- **自动更新。** 命令行用 `qialike upgrade`，TUI 内用 `/upgrade`，启动后不久还会做一次后台检查。
  minor 版本只提示、不静默自装；patch 静默安装。`QIALIKE_DISABLE_AUTOUPDATE=1` 可整体关闭，
  且自动更新失败绝不会打断你的会话。
