# 安全政策（Security Policy）

[English](SECURITY.md) | 中文

qialike 跑的是一个会读你文件、改你文件、在你机器上执行命令的 AI 代理。安全属性因此是产品的一部分，
而不是事后补丁：沙箱各档、审批边界、凭据存储与插件信任台账都写在
[README](README.zh.md) 的「安全边界」一章里；能越过其中任何一条的报告，正是本文要接的东西。

## 支持的版本

修复以**新发布产物**的形式交付。本项目没有补丁分支、不做回移植：产品按平台以单文件二进制分发，
所以修复到达你的方式就是装上那个新版本。

| 版本 | 是否支持 |
| --- | --- |
| 最新发布版 | ✅ |
| 上一个 minor 版本 | ✅（仅安全修复） |
| 更早的版本 | ❌ |

升级是原地的（`qialike upgrade`，或重跑安装器），不会动你的会话与设置。见 README 的「更新」一章。

## 如何报告漏洞

**安全问题请勿开公开 issue。**

1. **首选** —— 通过本仓库的 GitHub Security Advisories 提交私密报告（"Security" → "Report a
   vulnerability"）。报告、讨论与修复在 advisory 公布前都保持私密。
2. 若无法使用 advisories，请通过项目官网公布的联系方式联系维护者，并在信中明确说明这是安全报告。

一份有用的报告应包含：

- 版本（`qialike --version`）与平台、终端、操作系统版本；
- 当前生效的沙箱档位 —— `bubblewrap`、`landlock`、`seatbelt`、Windows ACL runner，或你**故意**
  切到的 `danger-full-access`；
- 你期望边界做什么、它实际做了什么，以及你手上最小的复现步骤。命令行、相关的
  `~/.dsh/qialike.log` 行和一份终端转录，胜过一段描述；
- 若是凭据或提示注入类问题：请说明载体是模型、工具结果、技能、插件，还是本地文件。

### 你会得到什么

| 阶段 | 目标时间 |
| --- | --- |
| 确认收到 | 3 个工作日 |
| 初步评估（严重性、受影响版本、能否复现） | 10 个工作日 |
| 修复或写明缓解办法 | 视严重性而定；我们会告知计划并同步进展 |

这是志愿者社区项目：**没有漏洞赏金**，上表是尽力而为的承诺而非合同义务。除你要求匿名外，
我们会在 advisory 中致谢。

## 范围

**在范围内** —— 本仓库及其发布产物中的任何问题：

- TUI 本体及其内置插件（`packages/qialike-app`、`apps/tui-bin`）；
- 各平台的沙箱与审批边界：Linux 的 bubblewrap 与内嵌 Landlock 启动器、macOS 的 Seatbelt、
  Windows 的 ACL 受限令牌 runner，以及 `workspace-write` / `read-only` / `danger-full-access`
  的策略阶梯；
- 凭据链路：API key 如何存储、如何解析，以及任何泄漏进转录区、会话文件或导出的路径；
- 插件信任台账，以及任何能让本地插件绕过显式信任决定运行的途径；
- 安装器与自更新链路 —— 校验和/签名处理、发布源选择、写进你 shell profile 的内容；
- 读栅栏：把 `.env`、`.git` 内部与凭据文件挡在模型视野之外的那道边界。

**不在范围内：**

- [DeepSeek Harness](https://github.com/deepseek-ai/deepseek-harness) 或任何第三方依赖自身的漏洞。
  请报给上游；但凡通过本仓库到达 qialike 用户的问题（升版本、加缓解、写明规避办法）我们仍会处理。
- **模型供应商**的 API 或你配置的网关自身的漏洞。qialike 是客户端；你的提示、代码与 key 会发往你
  选择的供应商，适用其条款。
- 位于**其他仓库**的网站或托管服务 —— 请报给该处公布的联系方式，并说明你指的是哪个项目。

## 属于设计而非漏洞的行为

以下是已写明的产品决策。这类报告会得到一句「见文档」的回复，而不是修复 —— 除非你能证明文档与行为不符：

- **`danger-full-access` 会够到工作区之外。** 这正是该档位的意义；它需要主动选择，README 写明了代价。
- **本地插件在本进程内以完整权限运行。** 因此才有显式、可撤销、绑定 harness 版本的信任决定
  （`qialike plugin trust`），以及任何一处编辑都会使其失效的哈希。
- **模型会看到你的代码，供应商会收到它。** 编码代理本就如此；README 的「数据与责任」一章直说了这件事。
- **你批准过的 shell 命令可以做到你批准的事。** 审批对话框就是边界；批准一条写到工作区之外的命令是
  一个决定，不是绕过。
- **在没有内核隔离的宿主上**，每一次 shell 调用都要逐个批准，而不是被静默放行 —— 这个降级形态是
  刻意的，并且会明确告知（`SANDBOX_UNAVAILABLE`）。

## 安全港（Safe harbour）

对于善意报告漏洞、只在**自己的**安装与数据上验证、并给我们合理修复窗口后才公开的人，我们不会追究
也不会支持对其进行法律行动。请不要测试他人的系统、不要外泄不属于你的数据、不要影响他人的服务。
